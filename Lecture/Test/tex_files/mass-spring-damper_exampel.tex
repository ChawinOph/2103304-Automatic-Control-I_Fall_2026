\documentclass[border=5mm]{standalone}
\usepackage{circuitikz}
\usetikzlibrary{patterns, decorations.pathmorphing}

\begin{document}
\begin{circuitikz}
    % Anchor/Wall
    \draw (0,0) node[anchor=east] {} to[short] (0,0);
    \draw[thick] (-0.5,0) -- (0.5,0);
    \draw[pattern=north west lines] (-0.5,0) rectangle (0.5,0.5);

    % Spring & Damper in parallel
    \draw (0,0) to[spring, l=$k$, *-*] (0,-2);
    \draw (1.5,0) to[damper, l=$c$, *-*] (1.5,-2);
    
    % Mass
    \draw (0,-2) node[mass, anchor=north] (M) {$m$};
    \draw (M.east) -- (1.5,-2); % connect damper to mass
    
    % Force arrow
    \draw[-latex] (-1,-2) -- (-1,-3) node[left] {$F(t)$};
\end{circuitikz}
\end{document}
